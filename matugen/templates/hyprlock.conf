background {
    path = {{image}}
    blur_passes = 1
    blur_size = 2
    brightness = 0.9
}

# Time
label {
    text = cmd[update:1000] echo "$(date +"%H:%M")"
    color = rgba({{colors.on_surface.default.hex_stripped}}FF)
    font_size = 90
    font_family = JetBrainsMono Nerd Font Bold
    position = 0, 180
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 8
    shadow_color = rgba(0,0,0,0.5)
}

# Date
label {
    text = cmd[update:60000] echo "$(date +"%A, %d %B %Y")"
    color = rgba({{colors.on_surface.default.hex_stripped}}CC)
    font_size = 18
    font_family = JetBrainsMono Nerd Font
    position = 0, 90
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 5
    shadow_color = rgba(0,0,0,0.4)
}

# Password input field
input-field {
    size = 280, 48
    outline_thickness = 2
    dots_size = 0.25
    dots_space = 0.4
    dots_center = true
    outer_color = rgba({{colors.primary.default.hex_stripped}}CC)
    inner_color = rgba({{colors.surface.default.hex_stripped}}CC)
    font_color = rgba({{colors.on_surface.default.hex_stripped}}FF)
    fade_on_empty = true
    placeholder_text = <span foreground="##{{colors.on_surface.default.hex_stripped}}">  Enter Password</span>
    hide_input = false
    rounding = 12
    check_color = rgba({{colors.primary_container.default.hex_stripped}}FF)
    fail_color = rgba({{colors.error.default.hex_stripped}}FF)
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    fail_transition = 300
    position = 0, -30
    halign = center
    valign = center
}

# Hint label
label {
    text =  Type to unlock
    color = rgba({{colors.outline.default.hex_stripped}}FF)
    font_size = 12
    font_family = JetBrainsMono Nerd Font
    position = 0, -90
    halign = center
    valign = center
}
